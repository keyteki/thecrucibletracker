CREATE TABLE deck_awards (
  id SERIAL PRIMARY KEY,
  type TEXT,
  player TEXT,
  deck_uuid TEXT,
  date_created TIMESTAMP,
  CONSTRAINT deck_awards_deck_player_constraint UNIQUE (player, deck_uuid)
);
